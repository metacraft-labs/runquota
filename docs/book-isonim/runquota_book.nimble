# Package

version       = "0.1.0"
author        = "Metacraft Labs"
description   = "The RunQuota book -- user documentation built on isonim-docs"
license       = "MIT"
srcDir        = "src"

# Dependencies

requires "nim >= 2.0.0"
requires "isonim >= 0.1.0"
requires "../../../isonim-docs"
